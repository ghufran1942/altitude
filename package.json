{
  "name": "altitude",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Altitude — a focus tool: Pomodoro timer, zoomable project hierarchy, and an anti-procrastination toolkit, with cross-device sync and native builds.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "assets": "capacitor-assets generate --iconBackgroundColor '#0F7C66' --iconBackgroundColorDark '#101820' --splashBackgroundColor '#0E5C4E' --splashBackgroundColorDark '#101820'",
    "cap:add:ios": "npm run build && npx cap add ios && npx cap sync ios",
    "cap:add:android": "npm run build && npx cap add android && npx cap sync android",
    "cap:sync": "npm run build && npx cap sync",
    "cap:ios": "npx cap open ios",
    "cap:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/local-notifications": "^6.1.0",
    "@supabase/supabase-js": "^2.45.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2",
    "vite-plugin-pwa": "^0.20.5"
  }
}
